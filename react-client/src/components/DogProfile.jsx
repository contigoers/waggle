import React from 'react';
import {} from 'antd';

// on component mount, ~get~ dog by id
class DogProfile extends React.Component {
    constructor(props) {
        super(props);
        this.state = {
        }
    }
}

const DogProfile = 

export default 